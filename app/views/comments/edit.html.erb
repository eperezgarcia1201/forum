<h1 class="text-header">Edit Comment </h1>

<%= simple_form_for ([@message, @comment]) do |f| %>
	<%= f.input :content, label: "Comments" %>
	<%= f.button :submit, :class => 'btn-custom' %>


<% end %>